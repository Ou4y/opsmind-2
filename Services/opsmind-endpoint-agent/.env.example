AGENTIC_AI_BASE_URL=http://localhost:4010
# Development only: paste a short-lived local JWT when running the endpoint agent manually.
# Never commit real JWTs or bearer tokens.
OPSMIND_JWT=<set-local-short-lived-jwt>
DEVICE_NAME=<set-local-device-name>
DEVICE_OS=<set-local-device-os>
HEARTBEAT_INTERVAL_MS=30000
POLL_INTERVAL_MS=10000
